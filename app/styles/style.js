import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    getFitText: {
        fontSize: 48,
        marginTop: 30,
        color: '#0037BA',
        fontWeight: 'bold'
    },
});